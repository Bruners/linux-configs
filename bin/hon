#!/bin/sh
HON='hon-x86'

if ps ax | grep -v grep | grep $HON > /dev/null
then
    pid=`ps aux | grep $HON | grep -v grep | awk '{print $2}'`
    echo "HoN is already running on $pid, killing process $pid"
    kill -9 $pid
    /games/HoN/hon.sh &
    exit 0
else
    echo "Starting HoN and mumble"
    /home/lasseb/.bin/vt &
    /games/HoN/hon.sh &
    exit 0
fi

